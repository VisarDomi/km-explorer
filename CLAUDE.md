# KM Explorer

Monorepo (npm workspaces). 4 packages: provider-types, extensions, server (port 29641), app (SvelteKit PWA, Svelte 5).
Video browser for ytboob.com. Uses Typesense API for listings, HTML scraping for video details and actor pages.
