use npx tsc --noEmit 2>&1 and npm run build to check your code.
read omp's docs for browser carefully before continuing because it is a high difficulty skill.
browser has .evaluate() for js and userscripts and you should use js inside it, not ts.
