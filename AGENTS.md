# KM Explorer

Monorepo. Packages: `provider-types`, `extensions`, `server` (port `29641`), `app` (SvelteKit PWA, Svelte 5).

Video browser for ytboob.com. Uses Typesense API for listings, HTML scraping for video details and actor pages.

## Read on demand

- Frontend app:
  `~/Documents/work/video/km-explorer/packages/app/AGENTS.md`
